Ext.data.JsonP.custom_datapoints({"guide":"<h1>Custom Data points</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/custom_datapoints-section-1'>The Alias API</a></li>\n<li><a href='#!/guide/custom_datapoints-section-2'>The Metadata API</a></li>\n<li><a href='#!/guide/custom_datapoints-section-3'>The Attach API</a></li>\n</ol>\n</div>\n\n<p>Apart from the standard data points, additional data points can be added. Checkout the below Api's</p>\n\n<h2 id='custom_datapoints-section-1'>The Alias API</h2>\n\n<p>Using this api, its possible to set an alias to the unique user identification used by the debuggify. This make it easy to map a debuggify user identifier to your site username or email id. Remember the old data will not have alias.</p>\n\n<pre><code>debuggify.alias('foo@example.com');\n</code></pre>\n\n<p>Its pretty straight forward to use the above api.</p>\n\n<p><strong>Note</strong> Make sure every unique user should be given the one alias to avoid any data inconsistency.</p>\n\n<h2 id='custom_datapoints-section-2'>The Metadata API</h2>\n\n<p>To attach custom data along with every message</p>\n\n<pre><code>debuggify.metadata({'login': true});\n</code></pre>\n\n<h2 id='custom_datapoints-section-3'>The Attach API</h2>\n\n<p>To attach custom data along with single message. This api need to be called from the logger object</p>\n\n<pre><code>var ajaxErrors = debuggify.Logger.get('ajax_errors');\n\najaxErrors.attach({\n  type: 'POST',\n  url: '/api/notification'\n});\n\najaxErrors.error('Error in Notification Api');\n</code></pre>\n\n<p>After calling attach, it must be followed by the one of the logging Apis like <code>.log</code>, <code>.error</code>, <code>.warn</code>, <code>.debug</code> or <code>.message</code> to push that data to the server.</p>\n\n<p><strong>Note</strong>: The attached data will expire after its once used by logging Api's. To send data with every request use <code>.metadata</code> api</p>\n","title":"Custom Data points"});